# AGENTS.md

## Cursor Cloud specific instructions

This repository (`telegramTestOne`) is currently an empty scaffold — it contains only a `README.md`. There is no source code, no dependency manifest, no build system, and no services to run.

### Current state (as of initial setup)

- **Languages/frameworks:** None yet.
- **Package manager / lockfile:** None.
- **Lint / test / build commands:** None available.
- **Services:** None to start.

### Future agents

Once code is added to this repository, update this section with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
- How to run lint, tests, and the dev server.
- Any required environment variables or secrets (e.g. a Telegram Bot API token).
